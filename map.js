Array.prototype.map(function (currentValue, index, array) {
    
})

const arr = [1, 2, 3, 4, 5];


arr.map(function (e, i) {
    return e % 2 === 0;
})

[false, true, false, true, false]

arr.map(function (e, i) {
    return `${i}番目の要素は${e}です`
})
